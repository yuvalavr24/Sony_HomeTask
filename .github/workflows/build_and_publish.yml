name: ci

on:
  push:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: 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

    - name: Build and push Docker image
      run: |
        docker buildx build --push --tag ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:latest -f Dockerfile.txt .
